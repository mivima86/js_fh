{
  "name": "08-crud",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server server_data/db.json --port 3001"
  },
  "devDependencies": {
    "json-server": "^0.17.2",
    "vite": "^4.1.0"
  }
}
